[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "norbok"
version = "0.1.0"
requires-python = ">3.9"
dependencies = ["requests"]

[project.scripts]
norbok = "norbok_pkg.main:run"
